services:
  brain:
    build: .
    container_name: assistant-brain
    ports:
      - "3000:3000"
    environment:
      HOST: "0.0.0.0"
      PORT: "3000"
      NODE_ENV: "production"
      BRAIN_API_KEY: "${BRAIN_API_KEY}"
    restart: unless-stopped